# Assumptions used for the task

- our data will fit into integers
- we don't want elaborative error processing and diagnostics (just fail on bad input)
- we're running on JVM so have access to System.in
